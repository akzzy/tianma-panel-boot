name: Fix Poco F1 Touchscreen (Strict Naming)

on:
  workflow_dispatch:
    inputs:
      tianma_boot_url:
        description: 'URL to the NEW Tianma Boot Image (Target)'
        required: true
        default: 'https://images.postmarketos.org/bpo/v25.06/xiaomi-beryllium/phosh/20251212-1422/20251212-1422-postmarketOS-v25.06-phosh-25-xiaomi-beryllium-tianma-boot.img.xz'
      ebbg_boot_url:
        description: 'URL to the EBBG Boot Image (Donor)'
        required: true
        default: 'https://images.postmarketos.org/bpo/v25.06/xiaomi-beryllium/phosh/20251212-1422/20251212-1422-postmarketOS-v25.06-phosh-25-xiaomi-beryllium-ebbg-boot.img.xz'

jobs:
  patch_kernel:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y device-tree-compiler python3 python3-pip wget xz-utils
          pip3 install --upgrade pip

      - name: Setup Tools
        run: |
          mkdir tools
          
          # 1. Download Magiskboot
          wget https://raw.githubusercontent.com/alitekin2fx/magiskboot/master/magiskboot -O tools/magiskboot
          chmod +x tools/magiskboot
          
          # 2. Create Extractor Locally
          cat << 'EOF' > tools/extract-dtb.py
          import sys
          import struct
          import os

          def extract(img_path, out_dir):
              if not os.path.exists(out_dir):
                  os.makedirs(out_dir)
              
              with open(img_path, 'rb') as f:
                  data = f.read()

              # DTB Magic Header: D0 0D FE ED (Big Endian)
              magic = b'\xd0\x0d\xfe\xed'
              offset = 0
              count = 0
              found_any = False
              
              while True:
                  offset = data.find(magic, offset)
                  if offset == -1: break
                  if offset + 8 > len(data): break
                      
                  size = struct.unpack('>I', data[offset+4:offset+8])[0]
                  if size <= 0 or offset + size > len(data):
                      offset += 4 
                      continue

                  with open(f'{out_dir}/{count:02d}.dtb', 'wb') as out:
                      out.write(data[offset:offset+size])
                  
                  offset += size
                  count += 1
                  found_any = True

              if not found_any:
                  print("No DTBs found in file.")
          
          if __name__ == "__main__":
              extract(sys.argv[1], sys.argv[2])
          EOF

      - name: Download Boot Images
        run: |
          wget "${{ github.event.inputs.tianma_boot_url }}" -O tianma.img.xz
          wget "${{ github.event.inputs.ebbg_boot_url }}" -O ebbg.img.xz
          unxz -f tianma.img.xz
          unxz -f ebbg.img.xz

      - name: Unpack Images
        run: |
          mkdir ebbg_work tianma_work
          
          cp ebbg.img ebbg_work/boot.img
          cd ebbg_work
          ../tools/magiskboot --unpack boot.img
          cd ..
          
          cp tianma.img tianma_work/boot.img
          cd tianma_work
          ../tools/magiskboot --unpack boot.img
          cd ..

      - name: Extract DTBs
        run: |
          mkdir ebbg_dtbs tianma_dtbs
          
          # Logic: If 'dtb' file exists, extract from it. If not, extract from 'kernel'.
          
          get_source() {
             if [ -f "$1/dtb" ]; then echo "$1/dtb"; else echo "$1/kernel"; fi
          }
          
          python3 tools/extract-dtb.py "$(get_source ebbg_work)" ebbg_dtbs
          python3 tools/extract-dtb.py "$(get_source tianma_work)" tianma_dtbs
          
          # Decompile
          for f in ebbg_dtbs/*.dtb; do dtc -I dtb -O dts "$f" -o "${f}.dts"; done
          for f in tianma_dtbs/*.dtb; do dtc -I dtb -O dts "$f" -o "${f}.dts"; done

      - name: Patch DTS (Swap Touchscreen Node)
        shell: python3 {0}
        run: |
          import os, re, sys

          def read_file(path):
              with open(path, 'r') as f: return f.read()
          def write_file(path, content):
              with open(path, 'w') as f: f.write(content)

          def get_node(content, name):
              # Find 'name {'
              pattern = re.compile(rf'({name}\s*\{{)')
              match = pattern.search(content)
              if not match: return None
              
              start = match.start()
              count = 0
              found = False
              for i, char in enumerate(content[start:], start):
                  if char == '{': count += 1; found = True
                  elif char == '}': count -= 1
                  if found and count == 0: return content[start:i+1]
              return None

          print("Finding EBBG Source Node...")
          source = None
          for f in os.listdir("ebbg_dtbs"):
              if f.endswith(".dts"):
                  c = read_file(f"ebbg_dtbs/{f}")
                  n = get_node(c, "touchscreen@38")
                  if n: source = n; break
          
          if not source: sys.exit("Error: touchscreen@38 missing in EBBG")

          print("Patching Tianma Target...")
          patched = False
          for f in os.listdir("tianma_dtbs"):
              if f.endswith(".dts"):
                  path = f"tianma_dtbs/{f}"
                  c = read_file(path)
                  target = get_node(c, "touchscreen@1")
                  if target:
                      write_file(path, c.replace(target, source))
                      patched = True
                      break # Only patch the first occurrence
          
          if not patched: sys.exit("Error: touchscreen@1 missing in Tianma")

      - name: Compile and Repack
        run: |
          # 1. Compile modified DTS -> DTB
          for f in tianma_dtbs/*.dts; do 
            dtc -I dts -O dtb "$f" -o "${f%.dts}"; 
          done
          
          # 2. STRICT NAMING: Concatenate all DTBs into 'dtb'
          # This overwrites the old 'dtb' file in tianma_work so magiskboot uses the NEW one.
          cat $(ls tianma_dtbs/*.dtb | sort) > tianma_work/dtb
          
          # 3. SAFETY CHECK: Did magiskboot extract 'dtb' separately or was it inside 'kernel'?
          # If it was inside kernel (no 'dtb' file initially), simply creating 'dtb' 
          # might cause magiskboot to append it to the existing kernel (duplicate DTBs).
          # We must strip the old DTBs from the kernel to be safe.
          
          # Helper to find DTB header offset (D0 0D FE ED)
          OFFSET=$(grep -a -b -oP '\xD0\x0D\xFE\xED' tianma_work/kernel | head -1 | awk -F: '{print $1}')
          
          if [ ! -z "$OFFSET" ]; then
             echo "Detected DTBs embedded in kernel at offset $OFFSET. Stripping them..."
             truncate -s $OFFSET tianma_work/kernel
          fi
          
          # 4. Save copy for user download
          cp tianma_work/dtb fixed_dtb.img
          
          # 5. Repack
          cd tianma_work
          ../tools/magiskboot --repack boot.img
          mv new-boot.img ../fixed_boot.img

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: fixed-files
          path: |
            fixed_boot.img
            fixed_dtb.img
            tianma_dtbs/*.dts
